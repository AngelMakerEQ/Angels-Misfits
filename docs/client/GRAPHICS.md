# Graphics

## Current State

No general graphics/rendering configuration (resolution, texture quality, DirectX settings, etc.) has been deliberately decided or documented yet — this document is a placeholder for that category specifically.

For classic-era visual restoration work already completed (zone files, spell effects, models, skeleton models), see `docs/architecture/CLIENT_ARCHITECTURE.md` — that work is tracked there, not here, since it's asset restoration rather than general graphics configuration.

For model-specific rendering (Luclin toggle, armor tinting), see `docs/client/MODELS.md`.

## History

If/when general graphics settings are deliberately configured, document them here and log the decision in **ADR-008** (or a new client ADR, per the project's stated preference for a single client ADR unless a change is large enough to warrant its own record).
